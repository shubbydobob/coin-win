# 생성물이지만 커밋한다 — 아니면 프론트를 빌드할 때마다 백엔드를 띄워야 한다.
# 낡았는지는 `.\gradlew.bat check` 가 검사한다. 근거: docs/spec/phase8-frontend.md § 5.2
# 손으로 고치는 파일이 아니므로 diff 에서 접는다.
frontend/openapi.json      linguist-generated=true
frontend/src/api/schema.d.ts linguist-generated=true
